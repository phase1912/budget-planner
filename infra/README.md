# infra

Local and deployment infrastructure: Docker Compose for PostgreSQL and MinIO,
environment definitions, and (later) deployment configuration.

Not yet populated — tracked by [F0.4](../docs/planning/backlog.yaml) (local development
environment) and [F0.7](../docs/planning/backlog.yaml) (configuration, secrets and
environments).
